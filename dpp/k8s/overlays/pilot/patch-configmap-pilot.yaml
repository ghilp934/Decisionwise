# patch-configmap-pilot.yaml
# Strategic Merge Patch: configmap.yaml
# - SQS_QUEUE_URL: ${AWS_ACCOUNT_ID} 제거 + production → pilot 큐
# - S3_RESULT_BUCKET: production → pilot 버킷
# - CORS_ALLOWED_ORIGINS: pilot 앱 도메인으로 교체 (배포 전 반드시 채울 것)
apiVersion: v1
kind: ConfigMap
metadata:
  name: dpp-config
  namespace: dpp-pilot
data:
  AWS_REGION: "ap-northeast-2"
  SQS_QUEUE_URL: "https://sqs.ap-northeast-2.amazonaws.com/783268398937/dpp-runs-pilot"
  S3_RESULT_BUCKET: "dpp-results-pilot"
  CORS_ALLOWED_ORIGINS: "https://app-pilot.decisionproof.io.kr"
  DPP_ACK_SUPABASE_NETWORK_RESTRICTIONS: "1"
  DPP_BILLING_PREFLIGHT_REQUIRED: "0"
