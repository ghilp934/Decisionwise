apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - api-deployment.yaml
  - reaper-deployment.yaml
  - configmap.yaml
  - worker-deployment.yaml
  - ingress.yaml
# NOTE:
# - namespace.yaml은 포함하지 말 것(프로덕션 네임스페이스 생성 방지)
# - secretproviderclass는 pilot/prod에서 objectName이 달라질 수 있으므로 base에 두지 말 것
